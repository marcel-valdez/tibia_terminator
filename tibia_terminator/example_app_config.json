{
  // Default process identifier to run against
  "default_pid": 8802,
  "configs": [
    // These values are obtained by running scanmem on the tibia process.
    {
      // PID of Tibia process
      "pid": 8802, // Update this!
      // [I32 I16 ]
      "mana_memory_address": "0x5672cf0", // Update this!
      // [I16 ]
      "speed_memory_address": "0x953f2f0", // Update this!
      // [I32 I16 I8 ]
      "soul_points_memory_address": "0x4bc2778", // Update this!
      // Not required, since it can be calculated.
      "hp_memory_address": null,
      // Not required, since it can be calculated.
      "magic_shield_memory_address": null,
      // Not required, since it can be calculated.
      "max_mana_address": null,
      // Not required, since it can be calculated.
      "max_hp_address": null
    },
    {
      // In Linux you can have multiple Tibia clients open and use a different
      // configuration for each one.
      // PID of Tibia process
      "pid": 8724,
      // [I32 I16 ]
      "mana_memory_address": "0x4b58eb0",
      // [I16 ]
      "speed_memory_address": null,
      // [I32 I16 I8 ]
      "soul_points_memory_address": "0x30abd6c",
      // Not required, since it can be calculated.
      "hp_memory_address": null,
      // Not required, since it can be calculated.
      "magic_shield_memory_address": null,
      // Not required, since it can be calculated.
      "max_mana_address": null,
      // Not required, since it can be calculated.
      "max_hp_address": null
    }
  ]
}
